<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sui="clr-namespace:SamsungUi.Controls">

    <!-- Base style for title bar buttons -->
    <Style x:Key="SamsungWindowButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OneUiTextPrimaryBrush}"/>
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <!-- Rounded background for hover -->
                        <Border x:Name="HoverBorder" 
                                Width="32" Height="32" 
                                CornerRadius="16" 
                                Background="Transparent" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True"/>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverBorder" Property="Background" Value="{DynamicResource OneUiControlBackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HoverBorder" Property="Background" Value="{DynamicResource OneUiSliderTrackBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Special style for the Close button (red on hover) -->
    <Style x:Key="SamsungWindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource SamsungWindowButtonStyle}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <Border x:Name="HoverBorder" 
                                Width="32" Height="32" 
                                CornerRadius="16" 
                                Background="Transparent" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center"
                                SnapsToDevicePixels="True"/>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="Content"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverBorder" Property="Background" Value="#E81123"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="HoverBorder" Property="Background" Value="#F1707A"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Style for the SamsungWindow -->
    <Style TargetType="{x:Type sui:SamsungWindow}">
        <Setter Property="Background" Value="{DynamicResource OneUiBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OneUiTextPrimaryBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI, Roboto"/>
        <Setter Property="WindowChrome.WindowChrome">
            <Setter.Value>
                <WindowChrome CaptionHeight="40" 
                              CornerRadius="0" 
                              GlassFrameThickness="1" 
                              UseAeroCaptionButtons="False" 
                              ResizeBorderThickness="6"/>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type sui:SamsungWindow}">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{DynamicResource OneUiControlBackgroundBrush}" 
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Barra del Titolo -->
                            <Grid Grid.Row="0" Background="Transparent">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icona Finestra -->
                                <Image Source="{TemplateBinding Icon}" 
                                       Margin="12,0,0,0" 
                                       Width="16" Height="16" 
                                       VerticalAlignment="Center" 
                                       WindowChrome.IsHitTestVisibleInChrome="True"/>

                                <!-- Titolo -->
                                <TextBlock Grid.Column="1" 
                                           Text="{TemplateBinding Title}" 
                                           VerticalAlignment="Center" 
                                           Margin="12,0,0,0" 
                                           FontSize="13"
                                           FontWeight="Medium"
                                           Foreground="{TemplateBinding Foreground}"/>

                                <!-- System Buttons -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top" Margin="0,4,4,0">
                                    <Button Style="{StaticResource SamsungWindowButtonStyle}" 
                                            Command="{x:Static SystemCommands.MinimizeWindowCommand}" 
                                            CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                        <Path Data="M 0,5 H 10 V 6 H 0 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" SnapsToDevicePixels="True"/>
                                    </Button>

                                    <Button x:Name="MaximizeButton" 
                                            Style="{StaticResource SamsungWindowButtonStyle}" 
                                            Command="{x:Static SystemCommands.MaximizeWindowCommand}" 
                                            CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                        <Path Data="M 0,0 H 10 V 10 H 0 Z M 1,1 V 9 H 9 V 1 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" SnapsToDevicePixels="True"/>
                                    </Button>
                                    
                                    <Button x:Name="RestoreButton" 
                                            Style="{StaticResource SamsungWindowButtonStyle}" 
                                            Command="{x:Static SystemCommands.RestoreWindowCommand}" 
                                            CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                            Visibility="Collapsed">
                                        <Path Data="M 2,0 H 10 V 8 H 8 V 10 H 0 V 2 H 2 Z M 3,1 V 7 H 9 V 1 Z M 1,3 V 9 H 7 V 3 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" SnapsToDevicePixels="True"/>
                                    </Button>

                                    <Button Style="{StaticResource SamsungWindowCloseButtonStyle}" 
                                            Command="{x:Static SystemCommands.CloseWindowCommand}" 
                                            CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                                        <Path Data="M 0,0 L 10,10 M 10,0 L 0,10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1" SnapsToDevicePixels="True"/>
                                    </Button>
                                </StackPanel>
                            </Grid>

                            <!-- Contenuto della Finestra -->
                            <AdornerDecorator Grid.Row="1">
                                <ContentPresenter />
                            </AdornerDecorator>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="WindowState" Value="Maximized">
                            <Setter TargetName="MaximizeButton" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="RestoreButton" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
