<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SamsungUi.Controls">

    <Style TargetType="{x:Type local:SamsungBadge}">
        <Setter Property="Background" Value="{DynamicResource OneUiControlBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource OneUiTextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,4"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SamsungBadge}">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="12"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Style: Primary -->
                        <Trigger Property="BadgeStyle" Value="Primary">
                            <Setter Property="Background" Value="{DynamicResource OneUiPrimaryBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <!-- Style: Success -->
                        <Trigger Property="BadgeStyle" Value="Success">
                            <Setter Property="Background" Value="#28C76F"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <!-- Style: Warning -->
                        <Trigger Property="BadgeStyle" Value="Warning">
                            <Setter Property="Background" Value="#FF9F43"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <!-- Style: Error -->
                        <Trigger Property="BadgeStyle" Value="Error">
                            <Setter Property="Background" Value="#EA5455"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
