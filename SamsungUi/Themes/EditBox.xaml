<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:SamsungUi.Controls">

    <Style TargetType="{x:Type local:SamsungEditBox}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource OneUiTextPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="BorderBrush" Value="{DynamicResource OneUiTextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Padding" Value="0,18,0,8"/>
        <Setter Property="MinHeight" Value="56"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:SamsungEditBox}">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <!-- Floating Hint -->
                            <TextBlock x:Name="HintText"
                                       Text="{TemplateBinding Hint}"
                                       Foreground="{DynamicResource OneUiTextSecondaryBrush}"
                                       FontSize="15"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Left"
                                       Margin="0,0,0,0"
                                       IsHitTestVisible="False"
                                       RenderTransformOrigin="0,0">
                                <TextBlock.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        <TranslateTransform X="0" Y="0"/>
                                    </TransformGroup>
                                </TextBlock.RenderTransform>
                            </TextBlock>

                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Text/Number/Email field -->
                                <TextBox x:Name="PART_TextBox"
                                         Grid.Column="0"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{TemplateBinding Foreground}"
                                         FontSize="{TemplateBinding FontSize}"
                                         VerticalAlignment="Bottom"
                                         CaretBrush="{DynamicResource OneUiPrimaryBrush}"
                                         Padding="0"
                                         Visibility="Visible"/>

                                <!-- Password field (masked) -->
                                <PasswordBox x:Name="PART_PasswordBox"
                                             Grid.Column="0"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{TemplateBinding Foreground}"
                                             FontSize="{TemplateBinding FontSize}"
                                             VerticalAlignment="Bottom"
                                             CaretBrush="{DynamicResource OneUiPrimaryBrush}"
                                             Padding="0"
                                             Visibility="Collapsed"/>

                                <!-- Reveal toggle button for Password -->
                                <ToggleButton x:Name="PART_RevealButton"
                                              Grid.Column="1"
                                              Width="24"
                                              Height="24"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Bottom"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand"
                                              FocusVisualStyle="{x:Null}"
                                              Visibility="Collapsed"
                                              IsChecked="{Binding IsPasswordRevealed, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Grid>
                                                <!-- Eye icon: visible when NOT revealed -->
                                                <TextBlock x:Name="EyeOpen"
                                                           Text="👁"
                                                           FontSize="14"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Opacity="0.5"/>
                                                <!-- Eye-off: visible when revealed -->
                                                <TextBlock x:Name="EyeClosed"
                                                           Text="🙈"
                                                           FontSize="14"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Visibility="Collapsed"
                                                           Opacity="0.5"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="EyeOpen" Property="Visibility" Value="Collapsed"/>
                                                    <Setter TargetName="EyeClosed" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                            </Grid>
                        </Grid>
                    </Border>
                    
                    <ControlTemplate.Triggers>
                        <!-- Focus state -->
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="{DynamicResource OneUiPrimaryBrush}"/>
                            <Setter TargetName="RootBorder" Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter TargetName="HintText" Property="Foreground" Value="{DynamicResource OneUiPrimaryBrush}"/>
                        </Trigger>
                        
                        <!-- InputType = Password -->
                        <Trigger Property="InputType" Value="Password">
                            <Setter TargetName="PART_TextBox" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="PART_PasswordBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_RevealButton" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <!-- InputType = Password && Revealed -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="InputType" Value="Password"/>
                                <Condition Property="IsPasswordRevealed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_TextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="PART_PasswordBox" Property="Visibility" Value="Collapsed"/>
                        </MultiTrigger>

                        <!-- Floating Hint State (Focus or Has Text) -->
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource Self}}" Value="False"/>
                                <Condition Binding="{Binding HasText, RelativeSource={RelativeSource Self}}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="HintText" Property="RenderTransform">
                                <Setter.Value>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                                        <TranslateTransform X="0" Y="0"/>
                                    </TransformGroup>
                                </Setter.Value>
                            </Setter>
                        </MultiDataTrigger>

                        <Trigger Property="HasText" Value="True">
                            <Setter TargetName="HintText" Property="RenderTransform">
                                <Setter.Value>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="0.75" ScaleY="0.75"/>
                                        <TranslateTransform X="0" Y="-16"/>
                                    </TransformGroup>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="HintText" Property="RenderTransform">
                                <Setter.Value>
                                    <TransformGroup>
                                        <ScaleTransform ScaleX="0.75" ScaleY="0.75"/>
                                        <TranslateTransform X="0" Y="-16"/>
                                    </TransformGroup>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
